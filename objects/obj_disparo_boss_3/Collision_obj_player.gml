instance_destroy();

if (!instance_exists(obj_zap)) {
	if (other.alarm[3] <= 0) {
		if global.dash_voa = false {
			global.hp -= global.dano_boss / global.armadura;
			global.sofrido += global.dano_boss / global.armadura;
		}
		other.alarm[3] = 1 + global.imunidade;
		other.image_alpha = 0.7
	}
} else {
	instance_destroy(obj_zap)
}