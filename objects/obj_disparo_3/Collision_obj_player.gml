instance_destroy();

if (!instance_exists(obj_zap)) {
	if (other.alarm[3] <= 0) {
		if global.dash_voa = false {
			global.hp -= (2 * global.dano_cosmevidro) / global.armadura;
			global.sofrido += (2 * global.dano_cosmevidro) / global.armadura;
		}
		other.alarm[3] = 1;
		other.image_alpha = 0.7;
	}
} else {
	instance_destroy(obj_zap)
}